# PROJETO — MVP do Guardião

> Cérebro do projeto. Fonte única de verdade sobre o Guardião.
> Este arquivo é lido no início de cada sessão e atualizado ao fim dela.
> Última atualização: 2026-07-15 (redesign da tela de abertura enviado pro GitHub, redeploy automático em andamento no Streamlit Cloud).

---

## 1. O que é o Guardião

Produto de IA que ajuda a pessoa a tomar melhores decisões de compra. O foco não é o menor preço, é a qualidade da decisão: comprar quando faz sentido, esperar quando não faz, e não cair em cilada.

A fundadora é solo e constrói o MVP com Claude Code. Regra guia: sempre a solução mais simples que valida a mesma hipótese.

## 2. O método (3 lentes)

1. Consciência de Compra → "Devo?" (necessidade x desejo, clareza, viabilidade financeira, estado emocional)
2. Comparador → "Qual?" (opções, critérios, preço entre alternativas)
3. Farejador de Ciladas → "É justo e seguro?" (fraude, abuso, orçamento de serviço tipo oficina)

## 3. Especificação Funcional v1.0 (independente de tecnologia)

- 6 dimensões: motivação, estado emocional, clareza, viabilidade, reversibilidade, risco externo.
- Tabela de decisão única com precedência.
- 6 vereditos: PODE COMPRAR, COMPARAR, CHECAR A OFERTA, ESFRIAR, REVER FINANÇAS, REPENSAR.
- Princípios: memória primeiro, veredito primeiro, retorno sempre disponível.

### Regras de apresentação da Consciência de Compra (marketing, não mexer sem decisão)
- Toda análise de compra mostra os TRÊS RÓTULOS, nesta ordem: **Motivação da compra**, **Clareza**, **Viabilidade financeira**. Todo o marketing da Consciência está construído em cima desses três rótulos. As 6 dimensões da spec ficam como evolução futura, não entram agora.
- Selo **"Não fecha ainda"** em AMARELO, no topo da resposta, aparece SÓ quando há problema (impulso, aperto financeiro, oferta ou vendedor a checar, desejo que bate com prioridade declarada, falta comparar). Serve pra chamar a atenção do usuário antes de fechar.
- NÃO existe selo de "pode fechar" nem nada verde. Quando está tudo certo, não aparece selo nenhum, vai direto pros três rótulos.

## 4. Diferencial central

Memória persistente. Os 3 GPTs originais da ChatGPT eram stateless. Sem memória, o produto é indistinguível do ChatGPT cru. Persistência é o objetivo do MVP e o real diferencial (DP-002 do Product Book).

## 5. Estado atual do MVP

Scaffold em `meus-produtos/guardiao-mvp/`, dentro do fluxo-criativo. Essa pasta é local e ignorada pelo git da mentoria: não é enviada pro repositório e não é tocada pelas atualizações da mentoria.
- Stack: Python + Streamlit + SQLite (memória em `guardiao.db`) + Anthropic SDK com tool use (`salvar_memoria`).
- Entrada por voz: microfone via `streamlit-mic-recorder` (a pessoa fala ou digita antes de comprar).
- Modelo padrão `claude-opus-4-8` (env `GUARDIAO_MODELO` troca por sonnet).
- Arquivos principais: `app.py`, `guardiao/metodo.py` (system prompt = método v1), `guardiao/memoria.py`, `guardiao/cerebro.py`.
- Roda com `streamlit run app.py` após `pip install -r requirements.txt` e chave `ANTHROPIC_API_KEY` no `.env`.
- Memória testada, persiste entre sessões.

## 6. Roadmap

- Dia 1 (feito): scaffold + memória persistente funcionando.
- Dia 2 (feito): 3 lentes afiadas + detecção de duplicata. Ver seção 9 para detalhes de implementação.
- Dia 3 (feito): onboarding + follow-up pós-compra. Ver seção 9 para detalhes de implementação.
- Dia 4 (deploy feito, PWA pendente): app no ar no Streamlit Community Cloud (`guardiao-mvp-gj28hwdwtmvrphfqnge2mm.streamlit.app`). Vercel NÃO serve app Streamlit (precisa de servidor Python vivo + WebSocket); ficou pra uma reconstrução futura em Next.js quando o produto validar. Detalhes na seção 9.
- Painel financeiro, Fase 2 (implementado e testado local, pendente de envio pro GitHub): tela "Meu painel" com termômetro do mês, de onde veio o gasto, dinheiro que o Guardião segurou e 6 padrões de consumo. Detalhes na seção 9 (2026-07-13).

## 7. Decisões abertas (da fundadora)

1. Monetização: assinatura x comissão de afiliado. Recomendação: assinatura, para preservar o posicionamento de confiança.
2. Escopo do Farejador no MVP. Recomendação: versão só com sinais visíveis.

## 8. Benchmark de concorrentes

Detalhes completos em `benchmark/benchmark-concorrentes.md`. Resumo:
- Concorrente pago em teste: R$ 49/mês, R$ 319/ano (promo). Onboarding: tela "me conhecer" → "quem sou eu" → tela de hábitos.

## 9. Histórico de decisões e aprendizados

- 2026-07-04: definido que a fonte única do projeto é a pasta do produto, trabalhada pelo Claude Code. O Project do claude.ai, se usado, é só espelho manual.
- 2026-07-04: o app foi movido de `~/guardiao-mvp` para `meus-produtos/guardiao-mvp/` dentro do fluxo-criativo, pra ficar junto dos produtos da mentoria e ter acesso aos agentes. A venv foi recriada no novo local (Python 3.14, Streamlit 1.58, Anthropic 0.116).
- 2026-07-04: entrada por voz adicionada ao app (microfone). Faltava o pacote `streamlit-mic-recorder`, que quebrava a abertura. Pacote instalado na venv e registrado no `requirements.txt`. App voltou a rodar.
- 2026-07-04: os três rótulos da Consciência de Compra (Motivação da compra, Clareza, Viabilidade financeira) tinham sido escondidos quando os 3 GPTs viraram um assistente só. A fundadora apontou que todo o marketing está em cima deles. Rótulos trazidos de volta ao formato de resposta (`metodo.py`). Combinado registrado na seção 3.
- 2026-07-04: combinado do selo amarelo "Não fecha ainda" (que só vivia numa conversa antiga e nunca tinha sido salvo) recuperado e implementado. Aparece em amarelo no topo só quando há problema; sem selo verde de "pode fechar". Render em amarelo feito no `app.py` (função `mostrar_resposta`).
- 2026-07-04: microfone reposicionado para dentro da própria barra de escrita, colado à esquerda do botão de enviar (efeito "ChatGPT", sem caixa nem borda própria). Implementado com `streamlit.components.v1.html` medindo a posição real do botão de enviar via JS e ajustando o microfone ao lado, com CSS injetado no iframe do `streamlit_mic_recorder` pra remover fundo e borda. Testado em mobile, desktop grande e tela padrão.
- 2026-07-04: separado o que persiste na tela do que persiste na memória. A tela agora abre LIMPA a cada uso (conversa nova), o rolo de mensagens antigas não aparece mais para o usuário. A continuidade vem da memória persistente (documento por trás), não do histórico visível. Implementado com marco de início de sessão (`mem.ultimo_id` + parâmetro `desde_id` em `ler_mensagens`, `cerebro.responder`). Quem o Guardião já conhece recebe um "oi de novo" curto em vez da pergunta de onboarding.
- 2026-07-04: botão "Reiniciar análise" na barra lateral (como tinha na Consciência original). Limpa a tela e começa uma análise nova sem fechar o app, mantendo a memória intacta. Usa o mesmo marco de sessão (`sessao_desde_id`).
- 2026-07-04: os três rótulos (Motivação da compra, Clareza, Viabilidade financeira) apareciam como um "textão" corrido, difícil de ler. A fundadora pediu para ficar visualmente claro que são três leituras distintas. Implementado no `app.py`: cada rótulo vira um cartão com borda própria, separado dos outros e da frase de ação final. O reconhecimento dos rótulos é feito no app (função `_extrair_rotulos`), não depende só do modelo formatar certo. `metodo.py` também passou a exigir uma linha em branco antes da frase de ação, pra ajudar a separação.
- 2026-07-04: a fundadora não queria chat de ida e volta sem fim. Decisão de produto: depois de cada diagnóstico, o app mostra 3 botões de decisão (Vou comprar / Vou esperar / Quero comparar antes). A pessoa clica, a decisão real dela (não só o veredito do modelo) fica registrada na memória via `mem.registrar_decisao`, e ela segue a vida. O campo de texto continua disponível se ela quiser detalhar mais, mas não é obrigatório.
- 2026-07-04: bug encontrado (fundadora testando ao vivo): `necessidades` sempre ficava vazio, tudo caía em `desejos`, mesmo casos claros de necessidade real (ex: celular com tela quebrada). Causa: o método nunca explicava a regra de quando usar cada lista. Corrigido em `metodo.py` com critério explícito (necessidade = problema real concreto; desejo = vontade/comparação/impulso) e regra de mover de desejos pra necessidades quando a clareza chega.
- 2026-07-04: bug mais sério encontrado no mesmo teste: uma conversa inteira (sobre um armário de escritório) não foi salva em NENHUM lugar da memória, o modelo simplesmente não chamou a ferramenta `salvar_memoria` naquele turno, apesar da instrução dizendo pra sempre salvar. Como a memória é o diferencial central do produto, isso não pode depender só do modelo lembrar. Corrigido com rede de segurança em `cerebro.py`: se a resposta trouxer um diagnóstico (os três rótulos) e o modelo não tiver chamado `salvar_memoria` naquele turno, o próprio código salva pelo menos o essencial (`_salvar_fallback_diagnostico`). A instrução em `metodo.py` também ficou mais imperativa ("obrigatório, não opcional").
- 2026-07-04: resumo dos botões de decisão também corrigido: antes pegava a última frase digitada (podia ser só "10" ou "sim", sem contexto), agora usa o texto da "Motivação da compra" do próprio diagnóstico, que descreve o item de verdade.
- 2026-07-04: cursor entra automaticamente no campo mais provável ao abrir a tela (nome, na tela de entrada; mensagem, na tela do chat). Implementado com `_autofoco` em `app.py`, via `components.html` medindo se algum campo já está focado antes de forçar o foco (não rouba foco se a pessoa já clicou em outro lugar).
- 2026-07-04: a fundadora reportou que o app pedia o nome de novo toda vez, mesmo no mesmo navegador. Causa: `st.session_state` vive só na memória do servidor, se reinicia (o que acontece bastante durante ajustes de código) ou o navegador perde a conexão, esquece geral. Corrigido: o nome agora é salvo em `st.query_params` (`?usuario=nome` na URL) ao entrar. Ao abrir o app, se a URL já tiver o parâmetro, reconhece direto sem perguntar. Testado inclusive com reinício completo do servidor, funciona.
- 2026-07-04: a lista de "necessidades" continuava vazia mesmo após a correção anterior, porque a rede de segurança (`_salvar_fallback_diagnostico`) sempre despejava tudo em "analises", sem classificar. Corrigido em `cerebro.py` com `_classificar_motivacao`: agora a própria rede de segurança decide necessidades x desejos pelo texto que o Guardião já escreveu ("necessidade" vs "desejo"/"vontade"). Migrada manualmente a entrada do armário que já estava presa em "analises" na conta real, agora aparece em "Necessidades" na barra lateral.
- 2026-07-04: a entrada salva pela rede de segurança não dizia qual produto era (só a motivação, tipo "necessidade real, tem coisa sem lugar pra guardar", sem nome do item). Corrigido: `_salvar_fallback_diagnostico` agora junta a última fala da pessoa (que cita o produto) com a motivação. Corrigida manualmente a entrada do armário já existente na conta real, que agora mostra "Armário (falta armário pra guardar as coisas)".
- 2026-07-04: início de exploração de design. A fundadora trouxe uma referência do Pinterest ("Genie AI Assistant App") e pediu uma tela de abertura limpa: fundo escuro, uma esfera 3D central (sem barra lateral, sem cartões, sem histórico visível), só a pergunta "O que você quer comprar hoje?". Conectado `OPENROUTER_API_KEY` no `.env` (modelo padrão `google/gemini-3.1-flash-image-preview`) para gerar a esfera como imagem de verdade (não CSS). Gerada a esfera final (azul + prateado, sem lavanda/rosa, mesma composição da referência) e salva em `meus-produtos/guardiao-mvp/assets/orb.png`.
- 2026-07-04: implementada a tela de boas-vindas (`_tela_boas_vindas` em `app.py`): aparece sempre que a sessão está sem histórico (análise nova), com fundo preto, a esfera `assets/orb.png` centralizada e a pergunta direta. Sem barra lateral, sem cartões. Assim que a pessoa manda a primeira mensagem (voz ou texto), a tela normal do chat assume (título, barra lateral, cartões, botões de decisão). O botão "Reiniciar análise" volta pra essa tela. O script de estilização do microfone foi extraído para `_estilizar_microfone(chave_widget)`, reusado nas duas telas.
- 2026-07-06: rodado teste completo de ponta a ponta (tela da bolha, diagnóstico com e sem problema, botões de decisão, reiniciar análise, reconhecimento por URL, classificação necessidades x desejos). Tudo funcionando, exceto um bug: quando a primeira mensagem da pessoa já chega combinada com a pergunta de "primeiro encontro" (prioridade), o aviso amarelo "Não fecha ainda" aparecia como texto solto em vez de caixa amarela, porque a detecção só olhava a primeira linha da resposta inteira. Corrigido em `mostrar_resposta` (`app.py`): agora procura o aviso em qualquer linha da resposta, não só na primeira, então funciona mesmo com saudação antes.
- 2026-07-06: decisão de produto sobre o botão de decisão "Vou comprar". A fundadora considerou e descartou um botão "Comprei" (confirmação pós-compra): pedir pra pessoa voltar depois e confirmar manualmente não é confiável, ninguém lembra. Mantido "Vou comprar" como registro de intenção no momento da decisão, que é quando a pessoa está de fato prestando atenção. A confirmação real de que a compra aconteceu (ou não) fica pro follow-up proativo do Dia 3 do roadmap (o Guardião pergunta depois de um tempo), não pra um botão que depende da pessoa lembrar.
- 2026-07-06: Dia 2 do roadmap implementado (3 lentes afiadas + detecção de duplicata). Antes, só a Consciência de Compra (DEVO?) tinha formato de verdade; Comparador (QUAL?) e Farejador (É SEGURO E JUSTO?) eram uma linha solta sem estrutura. Agora:
  - Dois cartões condicionais novos (só aparecem quando fazem sentido, sempre na mesma posição: depois de Clareza, antes de Viabilidade financeira): **Comparação** (quando a pessoa cita 2+ opções concretas) e **Checagem de segurança** (quando tem link, oferta, orçamento de serviço ou vendedor envolvido). `metodo.py` ganhou os critérios e exemplos; `app.py` estendeu `ROTULOS_CONSCIENCIA` e o regex de extração pra reconhecer os 5 rótulos possíveis.
  - Detecção de duplicata: regra explícita no método pra comparar todo item novo com o que já existe na memória (compras, desejos, necessidades, análises) e avisar em primeiro lugar quando achar parecido, antes do resto do diagnóstico.
  - Bug encontrado e corrigido durante o teste: a resposta cortava no meio (max_tokens=700 não era mais suficiente com até 5 cartões possíveis). Aumentado pra 1200 em `cerebro.py`.
  - Testado ao vivo: comparação entre dois notebooks, checagem de segurança de uma oficina suspeita (sem CNPJ, Pix adiantado), e detecção de duplicata (mesmo conserto de tela perguntado duas vezes). Os três cenários funcionaram corretamente.
- 2026-07-06: tela de abertura mudou de visual. A fundadora pediu pra tirar a esfera 3D e adotar o padrão Claude/ChatGPT: saudação por horário do dia + barra de escrita centralizada verticalmente na tela (não mais fixa no rodapé). Fundo voltou a ser claro (removido o preto). Implementado em `_tela_boas_vindas` (`app.py`). A imagem `assets/orb.png` não é mais usada no app (arquivo continua salvo, caso volte a ser usada em outro lugar no futuro).
- 2026-07-06: saudação ajustada de novo. Em vez de "Bom dia/Boa tarde/Boa noite" (por horário), a fundadora preferiu algo fixo e afetivo: "Oi, {nome}. Vamos cuidar do seu dinheiro." em fonte menor e mais suave, com "O que você quer comprar hoje?" logo abaixo, maior e em negrito. Reforça a parceria (o "vamos" junto) em vez de só marcar a hora do dia. Frase final calibrada com o nome real da fundadora (Lay) pra caber numa linha só.
- 2026-07-06: a fundadora recuou de novo, dessa vez rejeitando o padrão Claude/ChatGPT: achou que a tela parecia genérica demais e podia fazer a pessoa se perguntar "por que não uso o Claude/ChatGPT direto". Voltamos pro fundo escuro com identidade visual própria. Ela também rejeitou a esfera 3D anterior e pediu pra usar os **tijolinhos do logo real da IAgilize** (a marca por trás do Guardião), aparecendo em sequência e se montando, representando o Guardião conhecendo a pessoa aos poucos.
  - Como a marca já é usada no marketing e não pode variar, os blocos foram **recortados pixel a pixel do arquivo real** (`assets/logoGuardiaoBolso.png`, o arquivo tinha ficado inacessível direto do iCloud Drive por permissão do macOS, a fundadora copiou pra `assets/` manualmente). Nada foi redesenhado por IA generativa, que distorceria a marca.
  - Análise por código (sem scipy, componentes conectados implementados na mão) revelou 5 blocos reais: 1 = quadrado laranja (topo direita), 2 = bloco em formato de L (navy), 3 = retângulo do meio (navy), 4 = quadrado inferior esquerdo (navy), 5 = barra larga inferior (navy). Confirmado com a fundadora via imagem numerada antes de implementar.
  - Ordem de aparecimento ajustada pela fundadora pra ser de baixo pra cima e da direita pra esquerda, com o quadrado laranja sempre por último: **5 → 4 → 2 → 3 → 1** (0.4s de intervalo entre cada um). Implementado em `_icone_iagilize_animado()` e os recortes salvos em `assets/bloco_1.png` a `bloco_5.png`.
  - Bug encontrado e corrigido no processo: o HTML da tela não renderizava (aparecia como texto cru), porque a indentação aninhada do f-string tinha 4+ espaços de recuo em algumas linhas, e o Markdown do Streamlit interpreta isso como bloco de código. Corrigido gerando o HTML sempre em uma linha só, sem indentação.
  - Segundo bug: contra o fundo preto da tela, as bordas suavizadas dos blocos (antialiasing feito pensando em fundo branco no arquivo original) criavam um halo claro ao redor de cada peça, parecendo soltas. Corrigido com um cartão branco (`border-radius:16px`) atrás só do ícone, mantendo o resto da tela escura.
  - No fim, a fundadora testou numa aba do navegador com cache antigo (viu a esfera, achou que eu tinha mudado tudo sem avisar) e decidiu **voltar pra esfera por enquanto**, deixando os blocos de lado por ora. `_tela_boas_vindas` voltou a usar `assets/orb.png`. O código dos blocos (`_icone_iagilize_animado`, `_BLOCOS_ICONE`, `_ORDEM_APARECIMENTO`, os recortes `assets/bloco_1.png` a `bloco_5.png`) continua no projeto, pronto pra retomar se ela quiser voltar a essa ideia depois.
- 2026-07-06: Dia 3 do roadmap implementado (onboarding + follow-up pós-compra).
  - **Onboarding em duas perguntas**: além da pergunta de prioridade (já existia), o Guardião agora faz uma segunda pergunta curta sobre dívida/meta/limite (guardrail), guardada em `perfil.guardrails`. Bug encontrado no meio do caminho: a condição da segunda pergunta dependia de "memória vazia", mas depois que a prioridade é salva a memória deixa de estar vazia, então a pergunta 2 nunca disparava. Corrigido em `metodo.py`: as duas condições agora são independentes (pergunta 2 olha só se `perfil.guardrails` está vazio, não se a memória inteira está vazia).
  - **Follow-up pós-compra proativo**: no início de toda conversa nova, o Guardião checa a memória por decisões em aberto (`esperou`, `vai comparar`, `comprou`) com data anterior a hoje, e pergunta sobre a mais recente antes de tratar qualquer assunto novo. Isso fecha o ciclo da decisão sem depender de um botão de confirmação (que já tínhamos descartado por não ser confiável).
  - Para isso funcionar, o modelo precisava saber "que dia é hoje" pra comparar com a data salva, o que não existia antes. Adicionado `HOJE E: {data}` na injeção de memória em `cerebro.py` (`_montar_mensagens`).
  - A instrução de follow-up não disparava de primeira (ficava enterrada perto do fim do prompt, competindo com muitas outras regras). Movida pra logo depois de "Memória primeiro", com linguagem mais imperativa e um exemplo completo. Testado com uma pendência inserida manualmente (data de 2 dias atrás): funcionou, a primeira coisa que o Guardião perguntou foi sobre a pendência, antes de qualquer saudação.
  - Testado de ponta a ponta com contas de teste isoladas: onboarding (2 perguntas, guardrail salvo corretamente) e follow-up proativo (pendência puxada primeiro). Ambos funcionando.
- 2026-07-06: teste de ponta a ponta cobrindo Dia 2 + Dia 3 juntos (onboarding, comparação, checagem de segurança, follow-up com pendência retroativa, detecção de duplicata). Todos os 5 cenários passaram. No caminho, achado mais um caso de resposta cortada no meio (limite de tokens): quando o turno combina follow-up de pendência OU detecção de duplicata junto com o diagnóstico completo de rótulos, 1200 tokens ainda não bastava. Aumentado para 2000 em `cerebro.py`. Reenviado o mesmo cenário depois do ajuste: resposta completa, sem corte.
- 2026-07-07: DEPLOY. App colocado no ar no Streamlit Community Cloud. Aprendizados importantes: (1) Vercel não hospeda Streamlit (só páginas estáticas / funções curtas; Streamlit precisa de processo Python vivo + WebSocket). (2) Streamlit Cloud gratuito só acessa repositório GitHub PÚBLICO, então o repo `lyzasc-droid/guardiao-mvp` teve que virar público (a chave da API NÃO está no repo, está nos Secrets, então o que fica exposto é só o código/método). (3) A chave vai nos "Secrets" do app (formato TOML `ANTHROPIC_API_KEY = "sk-ant-..."`); erramos várias vezes por aspas sobrando e por uma versão mascarada/encurtada da chave ter sido colada. (4) Ao trocar o secret, o app precisa de REBOOT pra pegar o valor novo (não basta salvar). (5) Banco `guardiao.db` no Streamlit Cloud é efêmero (some quando o app hiberna), então a memória permanente de verdade fica pra quando migrar pra banco externo. Limitação de privacidade anotada: identificação é só um nome na URL, sem senha, então não é cofre por pessoa ainda (login real = passo futuro).
- 2026-07-07: onboarding reformulado de conversacional (LLM perguntava em texto aberto) para FORMULÁRIO OBJETIVO, por feedback da fundadora testando ao vivo: campo aberto trava quem não é organizado. Nova tela de "perfil rápido" no primeiro uso (`_tela_perfil_rapido` em `app.py`): duas perguntas com opções pra marcar (checkbox) + campo aberto opcional. A opção "Juntar dinheiro pra um objetivo" abre um campo condicional "Qual objetivo?" quando marcada (evita objetivo solto sem nome). Resultado salvo em `perfil.prioridade` e `perfil.guardrails`. `metodo.py` atualizado pra o LLM NÃO repetir perguntas de onboarding (agora vem tudo preenchido do formulário). Quem já tinha perfil (usuários antigos) não vê o formulário. Testado: formulário aparece, campo condicional surge, opções salvam, fluxo segue pro chat; lógica de montagem do texto verificada isolada (inclui o objetivo nomeado).
- 2026-07-07: identidade visual. A fundadora pediu a logo da marca no app e fundo branco. Tema claro fixo em `.streamlit/config.toml` (`base = "light"`, `primaryColor = "#E8853A"`, o laranja da marca). Substituído o antigo título "🛡️ Guardião" no topo das telas (`_cabecalho_logo`) e a esfera azul na abertura pela marca. A esfera (`orb.png`) e os blocos animados seguem guardados caso queira retomar.
- 2026-07-08: refino da marca. A fundadora pediu pra usar SÓ o símbolo (os tijolinhos), SEM a palavra "IAgilize", e o nome "Guardião do Bolso" ao lado. Recortado o símbolo isolado em `assets/icone.png` (sem o texto IAgilize). `_logo_html` agora monta símbolo + texto "Guardião do Bolso" (navy #131B3A). Aplicado no header das telas e na tela de abertura. Testado: perfil, abertura e header do chat mostram "[símbolo] Guardião do Bolso" em fundo branco.
- 2026-07-08: aplicado o Design System oficial da marca (peça visual trazida pela fundadora: paleta, lockup, cartão de veredito). Isso evolui a decisão anterior (só símbolo, sem "IAgilize"): o design system oficial usa o lockup completo "Guardião do Bolso" + "by IAgilize" em laranja com um tracinho de assinatura embaixo, deixando explícita a relação produto-filho/marca-mãe. Aplicado em `app.py`:
  - Paleta oficial extraída como constantes (`COR_NAVY #0C1D2E`, `COR_LARANJA #FF8A00`, `COR_OFFWHITE #F7F7F2`, `COR_VERDE #2BA745`, `COR_AMARELO #F5A623`, `COR_VERMELHO #EF4444`, `COR_AZUL_INFO #2F6DF6`).
  - `_logo_html` reformulado: símbolo + "Guardião do Bolso" (navy, negrito) + "by IAgilize" (laranja, caixa alta, menor) + barrinha laranja de assinatura embaixo.
  - Os cartões dos rótulos da Consciência de Compra (Motivação, Clareza, Comparação, Checagem de segurança, Viabilidade) agora seguem o estilo "cartão de veredito" da peça: fundo navy, rótulo em laranja caixa-alta, texto em off-white. Antes eram cartões brancos com borda simples do Streamlit.
  - O aviso "Não fecha ainda" mudou de amarelo (padrão do `st.warning`) para vermelho (`COR_VERMELHO`), batendo com o padrão de cores da peça (vermelho = alerta forte, amarelo reservado pro status "espere").
  - `.streamlit/config.toml`: `primaryColor` ajustado para o laranja exato da marca (`#FF8A00`, antes `#E8853A`) e fundo/texto travados em off-white (`#F7F7F2`) e navy (`#0C1D2E`).
  - Texto do conteúdo renderizado nesses cartões (`html.escape`) por segurança, já que o app está público sem autenticação e o conteúdo vem de texto digitado pela pessoa.
  - Testado com conta de teste isolada (`teste-marca-2`, removida do banco depois): tela de perfil, tela de abertura e cartão de veredito completo (com aviso vermelho + 3 lentes) confirmados visualmente batendo com a peça de referência.
- 2026-07-08 (mesmo dia, revisão): a fundadora testou o design system claro/navy (item anterior) no app publicado e não gostou, especificamente dos cartões de diagnóstico e da paleta em geral. Perguntada sobre a direção, escolheu "glassmorphism / cartões flutuantes" entre 3 linhas visuais oferecidas (escuro tech, claro minimalista, glass). Reformulado em `app.py` e `.streamlit/config.toml`:
  - Tema do app trocado de claro para **escuro fixo** (`base = "dark"`, fundo `#0B0F1A`, `secondaryBackgroundColor #141B2E`, texto `#F2F3F5`, `primaryColor` laranja da marca mantido).
  - Adicionado `_injetar_estilo_global()`: gradiente radial sutil (laranja + azul da marca) atrás do conteúdo, dando profundidade/"clima tech" ao fundo escuro.
  - Cartões de diagnóstico (Motivação/Clareza/Comparação/Checagem/Viabilidade) trocados do navy sólido anterior para **vidro fosco de verdade**: fundo semitransparente (`rgba(255,255,255,0.06)`), `backdrop-filter: blur(16px)`, borda clara sutil, sombra suave, flutuando sobre o fundo escuro com gradiente.
  - Aviso "Não fecha ainda" seguiu o mesmo tratamento (vermelho translúcido com blur, em vez de vermelho sólido).
  - Logo: título "Guardião do Bolso" mudou de navy para texto claro (`#F2F3F5`), pra ficar legível no fundo escuro; "by IAgilize" continua laranja.
  - Testado com conta de teste isolada (`teste-marca-3`, removida do banco depois): tela de perfil, tela de abertura (fundo escuro com brilho) e os 3 cartões de vidro fosco no diagnóstico completo, confirmados visualmente.
- 2026-07-09: fundadora trouxe a peça oficial do Design System (Guardião do Bolso by IAgilize): marca mãe x produto filho, 3 opções de lockup, paleta completa, tipografia (Sora para títulos, Manrope para texto), ícones, componentes e exemplos de aplicação. Corrigido o problema apontado por ela: o símbolo (`assets/icone.png`, blocos navy + laranja) ficava sem contraste no fundo escuro do app, porque os blocos navy se misturavam com o fundo navy. O Design System já previa isso na seção "Logo para fundo escuro (versão negativa)", recomendando blocos brancos sobre fundo azul-marinho. Gerado `assets/icone-negativo.png`: mesma imagem, só trocando os pixels navy por off-white (`#F7F7F2`) pixel a pixel (preserva alpha e o bloco laranja intacto), sem redesenho por IA, mesmo padrão usado antes para os blocos recortados. `_logo_html` em `app.py` passou a usar esse ícone. Testado visualmente no app rodando: blocos brancos legíveis, laranja preservado.
  - Pendências identificadas na peça, ainda NÃO aplicadas (aguardando decisão da fundadora sobre escopo):
    - Tipografia: o Design System define Sora (títulos) e Manrope (texto), o app hoje usa a fonte padrão do Streamlit.
    - Pequena divergência de cor: `COR_AZUL_INFO` no código está `#2F6DF6`, a peça mostra `#2F60F6` (diferença mínima, usada só no brilho de fundo).
    - O cartão de veredito da peça tem um formato diferente do implementado hoje: traz barras de intensidade por rótulo e um "selo de veredito" nomeado (ex: "ESPERE 48H", "PODE COMPRAR", "TROQUE A OPÇÃO") como estado permanente do card, além da lista de 5 rótulos (Motivação, Clareza, Comparação, Checagem, Viabilidade) que o app já tem hoje. Isso se conecta à decisão aberta da seção 7 sobre os 6 vereditos da spec v1.0, que ainda não têm essa forma visual implementada.
- 2026-07-09 (mesmo dia): a fundadora notou que os avatares do chat (um "quadradinho vermelho com carinha" pro usuário e um "quadradinho laranja com robozinho" pro Guardião) pareciam ultrapassados, e perguntou se precisava regenerar alguma imagem no ChatGPT. Não precisava: eram os ícones padrão do próprio Streamlit (`face` e `smart_toy`, do Material Symbols), que aparecem quando `st.chat_message` não recebe um avatar customizado, não uma imagem gerada. Primeira tentativa: símbolo da marca (`icone-negativo.png`) pro assistente + emoji pro usuário.
- 2026-07-09 (mesmo dia, refinamento): a fundadora achou a logo no avatar "parada" e com distorção de tamanho. Diagnóstico: o símbolo da marca é retangular (110x98) e o Streamlit espreme o avatar num círculo pequeno, cortando os blocos; além disso a logo repetida em cada mensagem (já presente no cabeçalho) vira ruído. Proposta a partir do Design System, escolhida pela fundadora entre 3 opções por lado:
  - **Guardião**: círculo navy com o quadradinho laranja da marca no centro, o elemento "ponto de decisão (checkpoint)" da seção Elementos Visuais da peça. Conta a essência do produto (o checkpoint antes da compra) e é geométrico, sem distorção. Gerado por código (PIL, desenhado em 512px e reduzido pra 128px) em `assets/avatar-guardiao.png`.
  - **Pessoa**: círculo cinza-azulado discreto com a inicial do nome dela (padrão dos apps modernos). Gerado em tempo de execução por `_avatar_usuario()` em `app.py` (cache em memória por letra; fonte com fallback Mac → Linux → padrão PIL, pro deploy no Streamlit Cloud funcionar).
  - Testado no app rodando com a conta real: "L" no lado da pessoa, checkpoint no lado do Guardião, ambos nítidos. As 4 mensagens de teste geradas durante a verificação (fone de ouvido e cadeira, ids 136 a 139) foram removidas do `guardiao.db`; a memória não foi tocada pelos testes (só houve perguntas de acompanhamento, sem diagnóstico, então nada foi salvo). Lição reforçada: testar sempre com conta isolada, não com a conta real.
- 2026-07-09 (mesmo dia): funcionalidade nova pedida pela fundadora, LISTA DE MERCADO. A pessoa fala itens pra anotar ("anota arroz, feijão e sabão em pó") e depois pede a lista pronta ("me manda a lista de mercado"). Implementação:
  - Chave nova `lista_mercado` no documento de memória (`memoria.py`, lista de strings simples), citada também na descrição da ferramenta `salvar_memoria` (`cerebro.py`) pra o modelo preservar a chave ao salvar.
  - Seção "Lista de mercado" no método (`metodo.py`): item de mercado do dia a dia NÃO passa por diagnóstico (sem rótulos, sem aviso amarelo, sem entrar em necessidades/desejos); anotar vários itens de uma vez; não duplicar item repetido; pedir a lista devolve lista simples; "já comprei" ou "tira" remove; "comprei tudo"/"limpa a lista" esvazia. Exceção de proteção: item caro ou fora do dia a dia (eletrônico, móvel) não é lista de mercado, cai no diagnóstico normal.
  - Barra lateral (`app.py`): seção "Lista de mercado" em "O que eu lembro de você".
  - Bug encontrado e corrigido no teste: a confirmação aparecia DUPLICADA na tela, porque às vezes o modelo escreve a frase, chama `salvar_memoria` e escreve a mesma frase de novo no turno seguinte do laço. Corrigido em `cerebro.py` ignorando bloco de texto idêntico a um já coletado no mesmo turno.
  - Testado de ponta a ponta com conta isolada (`teste-lista`, removida do banco depois): anotar 3 itens de uma vez, pedir a lista (veio limpa, em tópicos, sem diagnóstico), remover item comprado ("já comprei o feijão" tirou só o feijão), persistência entre sessões (a lista sobreviveu a reinício do servidor) e exibição na barra lateral. Tudo funcionando.
- 2026-07-09 (mesmo dia, reformulação): a fundadora testou ao vivo e o Guardião respondeu um pedido de lista puxando a pendência do armário (follow-up de pendências obrigatório do método). Ela definiu a regra de produto: **lista de mercado é armazenamento puro, não entra LLM na conversa**. Primeiro se identifica o tipo de pedido; se for lista, é só operação de itens (incluir, tirar, limpar, mostrar), persistida e recuperada. Reformulado:
  - `app.py` ganhou um roteador determinístico (`_resposta_lista_mercado` + `_extrair_itens_lista`): toda mensagem que menciona "lista" é tratada 100% em código, direto no banco, sem chamada de API. Reconhece adicionar ("anota/põe/inclui... arroz, feijão e sabão em pó"), remover ("tira/já comprei..." com casamento parcial: "tira o sabão" acha "sabão em pó"), limpar ("limpa/zera a lista", "comprei tudo") e leitura (qualquer menção à lista sem verbo de mudança). Comparação de itens ignora acento e maiúscula, não duplica item repetido. Resposta instantânea, custo zero, nunca bloqueia no teto diário (o atalho roda antes da checagem de limite; a mensagem ainda conta no contador de uso).
  - Custo por operação de lista caiu de US$ 0,04 a 0,10 (modelo Opus reescrevendo a memória inteira pra anotar 1 item) para zero.
  - Como retaguarda pra frases que não mencionam "lista" (ex: "lembra de comprar leite"), o modelo ganhou a ferramenta leve `atualizar_lista_mercado` em `cerebro.py` (ações adicionar/remover/limpar, mexe só na lista, sem reescrever o documento de memória) e a instrução em `metodo.py` de nunca usar `salvar_memoria` pra lista e de NÃO disparar follow-up de pendências nem duplicata em pedido de lista.
  - Efeito colateral aceito: se a sessão começa com uma operação de lista, o follow-up de pendências não dispara naquela sessão nem quando vier uma compra depois (o modelo já não vê a compra como primeira mensagem). A pendência volta a ser puxada na próxima sessão que começar com compra.
  - Testado reproduzindo o cenário exato do bug (conta isolada `teste-lista` semeada com pendência de armário de ontem, removida do banco depois): "anota aí na lista de mercado: arroz, feijão e sabão em pó" como primeira mensagem respondeu só "Anotado: ...", instantâneo, sem armário. "quero comprar uma cafeteira de 400 reais" na sequência caiu no fluxo normal (aviso, rótulos, botões, salvou em Desejos). Parser validado isolado com 11 frases variadas, incluindo "quero comprar um armário" corretamente ignorada pelo atalho.
- 2026-07-09 (mesmo dia, correção): na conta real, o Guardião salvou um aspirador de pó (descrito por voz, com detalhes: potente, sem fio, filtro EVA, pra colchão/sofá/carro) dentro de `lista_mercado`, junto com arroz e feijão. Não veio do roteador determinístico do app (a frase não continha a palavra "lista"), veio da própria ferramenta do modelo (`atualizar_lista_mercado`): o modelo confundiu um eletrodoméstico com item de mercado. A fundadora foi clara: o software tem que ser capaz de distinguir isso, não só a instrução do método.
  - Dado real corrigido direto no banco: item removido de `lista_mercado`, recriado em `necessidades` com o texto completo preservado.
  - `metodo.py`: critério explícito de lista de mercado (item curto, 1 a 3 palavras, comida/bebida/limpeza/higiene) com contraste direto: descrição longa, com detalhe/característica/motivo, NUNCA é lista de mercado mesmo que a pessoa diga a palavra "lista"; eletrodoméstico/eletrônico/móvel/roupa de marca vai sempre pro diagnóstico normal.
  - Freio no código (não depende só do modelo obedecer a instrução): `_parece_bem_duravel()` em `cerebro.py`, heurística de tamanho (mais de 5 palavras ou 35 caracteres = provável bem duravel descrito por extenso, não item de mercado). Aplicado em dois pontos: na ferramenta `atualizar_lista_mercado` (rejeita o item com `is_error` e instrui o modelo a tratar como compra normal, sem marcar `salvou_memoria`) e no roteador determinístico do `app.py` (item suspeito faz o atalho devolver `None`, caindo no fluxo normal do modelo em vez de silenciosamente entrar na lista).
  - Limitação assumida: a heurística é de tamanho, não um dicionário de palavras-chave (evita whack-a-mole enumerando todo eletrodoméstico possível). Pega o caso real que aconteceu (descrição longa por voz), mas uma menção curta tipo só "liquidificador" ainda depende do bom senso do modelo via `metodo.py`, não do freio de código.
  - Testado isolado: heurística correta em 8 casos (itens curtos passam, a frase real do aspirador é pega); ferramenta rejeita o aspirador longo e aceita arroz/feijão normalmente; roteador do app devolve `None` pro aspirador longo (cai no modelo) e continua anotando "arroz e feijão" direto.
- 2026-07-10: critério da lista de mercado fechado com a fundadora em 4 categorias: alimentos (inclui fruta, legume, verdura, carne), bebida, produto de limpeza, produto de higiene pessoal. `metodo.py` reescrito com essas categorias explícitas em vez da descrição vaga anterior ("comida, bebida, limpeza, higiene").
- 2026-07-10: **migração da memória de SQLite local pro Supabase (Postgres)**, resolvendo a limitação anotada desde o deploy (07/07): o `guardiao.db` no Streamlit Cloud é efêmero, some quando o app hiberna, então a memória (o diferencial central do produto) não era de fato permanente em produção.
  - Decisão da fundadora: **não migrar os dados existentes**. A conta real (`lay`) começa do zero no Supabase; o `guardiao.db` local continua no disco como histórico, mas não é mais lido pelo app. Script de migração de dados chegou a ser escrito e foi removido a pedido dela.
  - `guardiao/memoria.py` reescrito inteiro pra falar com Postgres via `psycopg2` em vez de `sqlite3`. Interface pública (nomes de função, parâmetros, retornos) mantida idêntica de propósito, então `app.py` e `cerebro.py` não precisaram mudar nada. Trocado o padrão de "abre e fecha conexão a cada chamada" (fazia sentido pro SQLite local) por uma conexão única reaproveitada pelo processo inteiro (`_conn()`, reconecta sozinha se cair), já que o app roda como servidor de vida longa e cada função sendo chamada várias vezes por turno tornaria o padrão antigo lento com a latência de rede até o Supabase.
  - Schema: `doc` virou `JSONB` (era `TEXT` com `json.dumps`/`json.loads` manual, psycopg2 serializa/desserializa sozinho via `psycopg2.extras.Json`), `id` de mensagens virou `BIGSERIAL`, upsert de memória via `ON CONFLICT ... DO UPDATE`, comparação de "hoje" pro teto diário feita explicitamente em UTC pra bater com o critério antigo do SQLite (`date('now')`).
  - Adicionada `psycopg2-binary` ao `requirements.txt` (tem wheel pronta pra Python 3.14, não precisou compilar nada).
  - **Trilha de erro na criação do projeto Supabase, registrada pra não repetir**: (1) reset de senha do banco não propagou na hora, duas tentativas com "password authentication failed" antes de funcionar; (2) depois disso, "Connection refused" na conexão direta (`db.<ref>.supabase.co:5432`) — causa real veio de um aviso da própria tela do Supabase: "Direct connections use IPv6 by default", e a rede local não estava roteando IPv6 bem. Resolvido trocando pra **Session Pooler** (`aws-1-sa-east-1.pooler.supabase.com:5432`, usuário no formato `postgres.<ref-do-projeto>`), que é IPv4 e é a alternativa oficialmente recomendada pelo próprio Supabase pra esse caso.
  - `DATABASE_URL` salva no `.env` local (nunca exibida em texto legível no chat, só recebida da fundadora e aplicada direto no arquivo). `.env.example` documentado com o formato esperado.
  - Testado local de ponta a ponta contra o Supabase de verdade (não um mock): todas as funções de `memoria.py` isoladas (salvar/ler memória, mensagens, contagem diária, registrar decisão), depois o app rodando via Streamlit com conta de teste isolada cobrindo os dois caminhos, o atalho determinístico da lista de mercado e o caminho que passa pelo modelo (compra de fone de ouvido, com a IA referenciando corretamente a prioridade salva). Contas de teste removidas do Supabase depois.
  - `DATABASE_URL` adicionada pela fundadora aos Secrets do Streamlit Community Cloud (mesmo padrão da `ANTHROPIC_API_KEY`), depois do `git push` do código (commit `6cccd36`). Primeira tentativa deu `ModuleNotFoundError: psycopg2` no app publicado porque o ambiente não tinha reinstalado as dependências novas do `requirements.txt` ainda. Resolvido com **Reboot app** manual no painel do Streamlit Cloud, forçando reconstrução completa do ambiente. Confirmado funcionando em produção: lista de mercado, memória de prioridade e persistência entre sessões, tudo lendo do Supabase.
- 2026-07-10: funcionalidade nova pedida pela fundadora, **PREÇOS DE REFERÊNCIA E COTAÇÕES**. Motivação dela: contexto de referência = item + preço + local + data ("paguei 10 no cotonete no Extra semana passada"), pra orientar troca de mercado e timing de compra. O caso guia foi a jornada real dela com o Vitanol: viu por 56 numa farmácia, achou caro contra a referência mental, esperou, achou por 43 no iFood e comprou.
  - Decisões de produto (dela): **preço de referência é só o que foi PAGO** (fato, não especulação); cotações vistas durante a caçada NÃO viram lista nova, moram **dentro do item em necessidades/desejos** (que já são a lista de intenção de compra) e morrem junto com a decisão.
  - Estrutura: chave `precos` na memória (histórico de pagos: item, preco, local, data; o último registro do item é a referência viva) e campo `cotacoes` dentro do item em necessidades/desejos. Data sempre posta pelo código, nunca pelo modelo.
  - Ferramenta leve `registrar_preco` em `cerebro.py` (mesmo padrão barato da lista de mercado): acao "pago" ou "cotacao". O retorno da ferramenta já devolve a comparação pronta (referência anterior + outras cotações do item), pro modelo orientar sem calcular. Cotação de item inexistente cria o item em desejos com nota de clareza pendente.
  - **Acao "pago" faz a transição completa do ciclo sozinha**: registra a referência, move o item de necessidades/desejos pra compras e encerra. Isso foi ajuste pós-teste: na primeira rodada o modelo ignorou a ferramenta e salvou a compra pelo `salvar_memoria` (caminho caro), deixando `precos` vazio e o item pendurado em necessidades. Corrigido consolidando a transição no código da ferramenta + instrução explícita no `metodo.py` de NÃO usar `salvar_memoria` pra compra com valor (com exceção anotada também na seção "Registrar na memória").
  - Barra lateral: cotações aparecem no rótulo do item ("vitanol (cotações: R$ 56,00 (Farmácia São João); R$ 43,00 (iFood))") e seção nova "Preços de referência" com o último pago de cada item. Bug visual achado e corrigido: dois cifrões na mesma linha viram fórmula matemática no Markdown do Streamlit; resolvido escapando `R\$`.
  - Testado de ponta a ponta com conta isolada (`teste-precos`, removida depois) reproduzindo a jornada do Vitanol: cotação 56 na primeira menção (item criado em necessidades com a cotação), cotação 43 com comparação automática ("bem mais barato que o da São João"), compra a 43 fechando o ciclo (referência criada, item movido pra compras, necessidades esvaziada). Executor da ferramenta validado isolado em 7 casos, incluindo entradas inválidas.
- 2026-07-10 (mesmo dia, refinamento): a fundadora testou o Vitanol de verdade e apontou uma dúvida de produto: recompra de item contínuo (remédio de uso contínuo, cosmético) precisa passar pelo diagnóstico completo de novo toda vez? Ela mesma resolveu: **toda primeira menção de qualquer item passa pelo crivo completo, sem exceção, nem indicação médica pula isso** ("necessidade" é uma resposta do diagnóstico, não um atalho que evita ele). Só DEPOIS que o item já tem preço de referência salvo (ou seja, já foi comprado e já passou pelo crivo uma vez), uma nova menção vira recompra: pula os rótulos e vai direto pra comparação de preço.
  - Nova seção "Recompra de item contínuo" no `metodo.py`: regra de ouro (primeira vez é sempre crivo completo) + o atalho (item com preço de referência em `precos` = recompra, usa `registrar_preco` e compara, sem repetir Motivação/Clareza/Viabilidade).
  - Testado de ponta a ponta com conta isolada (`teste-recompra`, removida depois): primeira menção do Vitanol (com indicação do dermatologista, comprado a R$ 45) recebeu o diagnóstico completo normal, com os três rótulos. Segunda menção do mesmo item, um preço mais alto visto em outra farmácia (R$ 68), pulou os rótulos e respondeu direto: "Bem acima da sua referência de R$ 45 na Pague Menos. Não fecharia por 68, procura em outra farmácia antes." Comportamento confirmado exatamente como a fundadora desenhou.
- 2026-07-10 (mesmo dia, correção de um gap real): a fundadora pediu uma lista separada de "cosméticos e medicamentos" pra registrar itens de uso contínuo, mas isso escondia um problema de verdade: como o atalho de recompra estava ligado só a "o item tem preço de referência", uma compra ÚNICA e pontual (ex: aspirador de pó) também dispararia o atalho numa menção futura do mesmo tipo de item, pulando o crivo indevidamente. Em vez de criar uma lista com nome fixo (que ficaria curta: e ração de pet, cápsula de café, lente de contato?), resolvido com uma marcação geral.
  - Campo novo `uso_continuo` (booleano) no preço de referência, preenchido pelo próprio modelo a partir do contexto da conversa (decisão da fundadora: automático, sem perguntar) quando registra uma compra ("pago"): true pra remédio de uso contínuo, cosmético do dia a dia, assinatura; false ou omitido pra compra pontual (eletrodoméstico, móvel, presente).
  - A regra de recompra em `metodo.py` foi apertada: só pula o crivo quando o item bate com uma referência que tem `uso_continuo: true`. Item com referência mas `uso_continuo: false` (compra pontual) passa pelo crivo completo de novo numa próxima menção, porque pode ser uma decisão nova (trocar de aparelho, presentear alguém).
  - Barra lateral (`app.py`): itens de uso contínuo ganham o selo 🔁 ao lado da referência, com legenda explicando o símbolo. Dá a visibilidade que a fundadora queria, sem precisar de uma lista separada.
  - Testado de ponta a ponta com conta isolada (`teste-continuo`, removida depois), dois cenários no mesmo fio: (1) Vitanol comprado com "uso contínuo, uso todo dia" no texto → marcado `uso_continuo: true`, selo 🔁 apareceu, e uma segunda menção pulou os rótulos direto pra comparação de preço. (2) Aspirador de pó comprado com "finalmente comprei, já fazia tempo que eu queria" → NÃO marcado como contínuo (sem selo), e recebeu o diagnóstico completo normal (Motivação, Clareza, Viabilidade). Os dois comportamentos bateram exatamente com o desenho.
- 2026-07-12: **bug de produção encontrado e corrigido**, achado testando a funcionalidade de preços direto no app publicado (não em local) com conta isolada. Na segunda mensagem, o app quebrou com `KeyError` na barra lateral. Causa: o modelo ignorou a ferramenta `registrar_preco` e usou `salvar_memoria` genérico, escrevendo `precos` com formato próprio (`"valor"` em vez de `"preco"`, sem `uso_continuo`) — mesma categoria de falha já vista com o aspirador de pó na lista de mercado (o modelo às vezes não obedece a instrução escrita, mesmo explícita).
  - Corrigido com o mesmo princípio já estabelecido no projeto (freio no código, não só na instrução): `_normalizar_precos()` em `cerebro.py`, chamada dentro de `_aplicar_salvar_memoria` antes de gravar, conserta qualquer registro de `precos` malformado (`valor`→`preco`, tipo numérico, defaults pra `local`/`data`/`uso_continuo`) não importa qual ferramenta o modelo usou. Barra lateral (`app.py`) também ficou defensiva com `.get()` e fallback, pra dado antigo já quebrado no banco não derrubar a tela.
  - `metodo.py` reforçado: preço, cotação ou compra com valor mencionado SEMPRE usa `registrar_preco`, nunca `salvar_memoria`, com o motivo explicado (salvar_memoria não sabe o formato de `precos`).
  - Ressalva anotada: mesmo com o crash resolvido, esse teste específico mostrou o modelo confundindo qual preço vira a referência (guardou o valor visto depois em vez do valor pago). Não quebra mais o app, mas é sinal de que a precisão da ferramenta `registrar_preco` ainda depende do modelo escolher chamá-la; vale observar se o problema se repete e, se sim, considerar um freio de código também pra esse caso (ex: detectar e recusar quando o modelo tenta gravar `precos` via `salvar_memoria`, forçando o uso da ferramenta certa).
  - Dado corrompido da conta de teste corrigido em produção com a função nova antes de confirmar visualmente (sem erro, `Preços de referência` renderizando normal). Conta de teste removida do banco depois.
- 2026-07-12: **fundação de dados pro dashboard futuro**. A fundadora quer um painel (limite mensal, divisão necessidade/desejo/impulso, dinheiro poupado, tendência de preço, padrões tipo "compra e não usa" ou "gasta além do que pode"). Antes de desenhar a tela, mapeado o que já existia vs. o que faltava, e implementados os 5 registros que faltavam:
  - **Limite mensal**: `perfil.limite_mensal` (novo campo). Duas portas de entrada, como planejado: campo numérico opcional no formulário de perfil rápido (`app.py`, `_tela_perfil_rapido`) e ferramenta nova `definir_limite_mensal` em `cerebro.py` pra quando a pessoa fala o valor numa conversa ("meu limite é 2500").
  - **Categoria automática da compra** (necessidade / desejo / impulso): `_aplicar_registrar_preco` agora decide sozinha, pelo código, não pelo modelo. Se o item veio de "necessidades" ou "desejos" (já tinha passado pelo crivo), herda essa categoria; se não tinha passado por lugar nenhum (a pessoa já chegou dizendo "comprei"), vira "impulso" automaticamente. É o dado estruturado por trás do "gastos que surgem do nada" que a fundadora descreveu.
  - **Forma de pagamento**: campos opcionais `forma_pagamento` ("vista"/"parcelado") e `parcelas` na ferramenta `registrar_preco`, preenchidos só quando a pessoa menciona espontaneamente (nunca perguntado de propósito).
  - **Gatilho emocional**: quando o modelo identifica um gatilho (ansiedade, euforia, compensação etc, o mesmo sinal que já dispara o aviso "Não fecha ainda"), grava num campo `gatilho` no item de necessidades/desejos; esse campo viaja automaticamente pro registro de compra quando o item é comprado (código, não depende do modelo lembrar duas vezes).
  - **Resultado da compra**: ferramenta nova `registrar_resultado_compra` (valeu / arrependimento / nao_uso), fecha o ciclo do follow-up de pendências que já existia mas não salvava a resposta em lugar nenhum. É o dado que destrava o padrão "compra e depois não usa" e o cruzamento impulso×arrependimento.
  - Barra lateral (`app.py`) ganhou a seção "Limite mensal" com barra de progresso (gasto do mês / limite).
  - Testado de ponta a ponta com conta fictícia (`beltrano-cruz`, removida do banco depois, seguindo a nova convenção de nome fictício em vez de "teste-x"): funções isoladas primeiro (limite, categoria impulso vs necessidade com gatilho herdado, resultado da compra com item inexistente/resultado inválido rejeitados corretamente), depois pelo app rodando de verdade — formulário com campo de limite funcionando, compra por impulso com parcelamento capturada certinho pelo modelo em conversa real ("parcelei em 3x" → `forma_pagamento: parcelado, parcelas: 3, categoria: impulso`), resultado de compra registrado mesmo fora do fluxo automático de follow-up (a pessoa comentou espontaneamente "me arrependi" e o modelo generalizou a instrução corretamente), barra de limite mensal renderizando sem bug de Markdown.
  - Ainda não migrado pro GitHub/produção. Falta ainda: desenhar a tela do painel em si (Fase 2), que só entra depois que a fundadora validar essa fundação.
- 2026-07-12 (mesmo dia, planejamento da Fase 2 fechado): plano da tela do painel discutido e fechado com a fundadora, **ainda não implementado**. Registro pra retomar em sessão nova sem perder o contexto.
  - **Navegação**: botão novo na barra lateral "📊 Meu painel", troca a tela principal (função nova, ao lado de `tela_do_chat`/`_tela_boas_vindas`), com "← Voltar pro chat" pra retornar. Não mexe no fluxo de conversa existente.
  - **4 blocos** (a fundadora cortou um 5º bloco "Preços em movimento" por ser redundante com o que já acontece ao vivo na conversa e com o que entra no bloco de padrões; a tendência de preço ficou embutida dentro de "recorrência esticando"):
    1. **Termômetro do mês**: gasto vs limite mensal, barra de progresso, dias restantes no mês. Convite discreto pra definir limite se ainda não tiver.
    2. **De onde veio o gasto**: barra dividida necessidade / desejo / impulso / mercado, valor e percentual, sobre as compras do mês.
    3. **Dinheiro que o Guardião segurou**: soma de (cotação mais cara vista − preço pago) por item comprado. O número de prova de valor do produto.
    4. **Padrões de consumo**, com um cuidado importante combinado: cada padrão só aparece quando tiver dado suficiente pra não mentir; abaixo do mínimo, mostra um aviso honesto tipo "ainda não tenho dado suficiente aqui, continue usando". Padrões e mínimos:
       - Comprador por impulso (% das compras que foram impulso): mínimo 3 compras no período.
       - Compra e abandona (% de resultado arrependimento/não_uso): mínimo 3 compras com resultado registrado.
       - Gasta além do que pode (limite + parcelas comprometidas em meses futuros): funciona com 1 compra parcelada, é matemática direta, sem mínimo.
       - Cruzamento impulso × arrependimento (o mais valioso, e o que mais precisa de volume): mínimo 5 compras por impulso avaliadas.
       - Recorrência esticando (preço subindo ou intervalo de recompra encurtando em item de uso contínuo): mínimo 2 compras do mesmo item (mínimo matemático).
       - Caçador de preço bom (economia acumulada comparando preço antes de comprar): puxa do bloco 3, sem mínimo adicional.
  - **Visual**: mesma linguagem já usada no app (fundo escuro, cartões em vidro fosco, navy e laranja da marca), construído nos mesmos moldes dos cartões de diagnóstico já aprovados, não gráfico de biblioteca genérica.
  - **Próximo passo real**: implementar a tela (`_tela_painel` em `app.py` + funções de cálculo dos 4 blocos), testar com conta fictícia, só então decidir sobre enviar a fundação (ainda pendente no GitHub) e o painel juntos ou em pushes separados.
- 2026-07-13: **Fase 2 do painel implementada e testada de ponta a ponta.** Antes de codar, fechado com a fundadora um ponto que não estava no plano original: a fatia "mercado" do bloco 2 não tinha nenhuma fonte de dado (a lista de mercado é armazenamento puro, sem preço). Decisão dela: preço por item da lista de mercado é só consulta pessoal (não conta como gasto); gasto de mercado só é registrado quando ela fala um valor avulso tipo "gastei 80 no mercado", sem vincular a item.
  - **Ferramenta nova `registrar_gasto_mercado`** em `cerebro.py` (mesmo padrão leve de `definir_limite_mensal`): grava um gasto avulso direto em `compras`, com `item: "mercado"`, `categoria: "mercado"`, sem vincular a nenhum item da lista. Ensinado em `metodo.py` (seção "Gasto avulso de mercado") a distinguir isso de `registrar_preco` (que é sempre item específico).
  - **Bug de dado encontrado e corrigido antes de existir uso real**: `_aplicar_registrar_preco` (ação "pago") movia o item de necessidades/desejos pra compras mas descartava o campo `cotacoes` do item no caminho. Isso quebraria silenciosamente o bloco 3 do painel (que depende da cotação mais cara vista por item comprado) assim que a fundadora começasse a usar de verdade. Corrigido preservando `cotacoes` na compra.
  - **`guardiao/painel.py` (novo módulo)**: funções puras (recebem o doc de memória, devolvem dado pronto pra tela, sem tocar banco nem Streamlit) para os 4 blocos: `calcular_termometro`, `calcular_origem_gasto`, `calcular_dinheiro_segurado`, `calcular_padroes` (6 padrões: comprador por impulso, compra e abandona, gasta além do que pode, impulso × arrependimento, recorrência esticando, caçador de preço bom). Validado isolado com doc sintético antes de tocar o app.
  - **`_tela_painel` em `app.py`**: botão "📊 Meu painel" no topo da barra lateral, tela cheia com os 4 blocos em cartões de vidro fosco (mesma linguagem visual dos cartões de diagnóstico), botão "← Voltar pro chat" que preserva a conversa exatamente como estava.
  - **Bug visual encontrado e corrigido em teste real**: o escape `R\$` (usado no markdown puro pra não virar fórmula matemática) mostrava a barra invertida literal na tela, porque os cartões do painel usam HTML bruto (`unsafe_allow_html`), que não reprocessa esse escape. `_reais()` do painel não usa mais o escape.
  - Testado de ponta a ponta com conta fictícia (`camila-ferraz`, removida do banco depois): funções de `painel.py` isoladas primeiro com doc sintético (todos os 4 blocos batendo com o cálculo manual), depois `_aplicar_registrar_preco` e `_aplicar_registrar_gasto_mercado` isolados contra o Supabase real (cotação preservada na compra, gasto de mercado avulso registrado certo), depois o app rodando de verdade (Streamlit local) com a conta fictícia populada com dado variado (compra com cotação, gasto de mercado, parcelamento ativo, item de uso contínuo com 3 preços): termômetro, origem do gasto, dinheiro segurado e os 6 padrões renderizaram com os números exatos esperados, e "Voltar pro chat" devolveu a conversa intacta.
  - **Refinamento visual (mesmo dia, depois de revisar rodando local)**: a fundadora mandou referência do Pinterest (dashboard fintech escuro com anéis/gauges) e pediu "fácil de ler mas bonito". Trocado: barra reta do termômetro por anel/gauge circular em SVG (percentual do limite no centro); barra empilhada de "de onde veio o gasto" por gráfico de rosca (donut) em SVG com o total no centro e legenda ao lado; padrões com percentual único (impulso, abandono, cruzamento) ganharam mini-barra de progresso embaixo do texto. Sem biblioteca nova (SVG puro, mesmo padrão já usado nos cartões de diagnóstico), paleta navy/laranja mantida. Revalidado com a mesma conta fictícia.
  - **Segunda rodada de refinamento visual (mesmo dia)**: primeira versão ainda pareceu "pobre" pra fundadora, ela apontou 3 gaps na comparação com a referência: layout empilhado (cartões um embaixo do outro em vez de grid), pouca densidade/variedade visual, e acabamento simples (sem ícone, sombra rasa). Resposta: reformulado `_cartao_painel` com fundo em gradiente sutil, borda superior colorida de destaque (accent bar), sombra mais profunda e badge circular de ícone por cartão. Layout virou grid de verdade com `st.columns`: termômetro, origem do gasto e dinheiro segurado lado a lado (3 colunas), padrões de consumo em grid 3x2 com tiles compactos (ícone, número grande, mini-barra quando aplicável) em vez de cartões de texto corrido em coluna única. Anéis/gauges reduzidos de tamanho pra caber nas colunas mais estreitas. Revalidado visualmente com a mesma conta fictícia.
  - **Terceira rodada (mesmo dia)**: mesmo com grid, ainda "morno" pra fundadora — faltava gradiente de verdade, tamanhos assimétricos entre cartões e mais diversidade de acabamento. Trocado o traço solido dos anéis/donuts por gradiente real via `<linearGradient>` em SVG (paleta `_GRADIENTES`, um id único por anel pra não colidir no DOM); números-hero (dinheiro segurado, comprador por impulso, caçador de preço bom) ganharam texto em gradiente (`background-clip:text`); cada cartão ganhou um brilho colorido desfocado no canto (cor do próprio tema do cartão). Layout deixou de ser grid uniforme: linha 1 é `st.columns([3,2])` (termômetro como hero grande à esquerda, origem do gasto + dinheiro segurado empilhados à direita); padrões viraram 3 linhas com larguras alternadas (2:1, 1:2, 1:1) em vez de 3 colunas iguais repetidas. Revalidado com a mesma conta fictícia.
- 2026-07-14 (quarta rodada de design, reescrita estrutural): mesmo com gradientes e colunas, a fundadora comparou de novo com a referência do Pinterest e apontou que continuava aquém. Diagnóstico: `st.columns` não entrega mosaico de verdade (espaçamento largo, caixas do mesmo tamanho, sem densidade), e o container central do Streamlit tem ~730px de largura máxima, sufocando qualquer grid. Reescrita:
  - **Painel virou UM bloco HTML com CSS grid de 12 colunas** (mosaico "bento"), abandonando `st.columns` por completo na tela do painel. Classes `sp3/sp4/sp6/sp8` controlam quantas colunas cada tile ocupa; media query empilha no celular. O `<style>` do painel também alarga `stMainBlockContainer` pra 1180px só enquanto o painel está na tela (o chat continua estreito).
  - **Orbe hero**: o termômetro virou uma esfera laranja brilhante (gradiente radial + halo de luz, vermelha quando estoura o limite) com o percentual dentro, o elemento mais marcante da referência.
  - **Gráfico grande novo**: "Gasto ao longo do mês", barras por dia do mês em SVG com gradiente (dias sem gasto viram tocos apagados, régua dia 1 / hoje / fim do mês). Precisou de dado novo: `calcular_gasto_diario` em `painel.py` (agrega compras do mês por dia).
  - **Padrões de %** (impulso, abandono, cruzamento) viraram mini-medidores radiais como os da referência; padrões bloqueados mostram anel cinza com o progresso até desbloquear (ex: "2/3 dados"), contando uma história em vez de só negar.
  - Layout final: orbe (4 col) + gráfico do mês (8 col) / donut (4) + dinheiro segurado (8) / 4 minis (3 cada) / parcelas (6) + recorrência (6).
  - Pegadinha registrada: o Streamlit recarrega `app.py` ao salvar, mas NÃO recarrega módulos importados (`guardiao/painel.py`) sem watchdog; a função nova só apareceu depois de reiniciar o servidor (o `KeyError: gasto_diario` no primeiro teste era isso, não bug de código).
  - Revalidado com conta fictícia (`camila-ferraz`, removida depois) em desktop (1280px, mosaico completo) e janela estreita (711px, tiles empilhados corretamente).
  - Ainda não enviado pro GitHub. Falta decidir com a fundadora se sobe junto com a fundação de dados (pendente desde 07/12) ou em pushes separados.
- 2026-07-14 (aprovação + ajuste de copy): fundadora aprovou o mosaico da quarta rodada ("pra essa primeira versão ficou bom"). Único ponto: a palavra "desbloquear" nos padrões sem dado suficiente soava como prêmio, mas o padrão por trás pode ser uma notícia ruim (compra por impulso, arrependimento) — estranho tratar isso como conquista. Trocado "Continue usando pra desbloquear" por "Ainda não tenho dado suficiente aqui" nos dois pontos do `_tile_padrao_html`, mantendo o anel cinza de progresso (a fundadora não pediu pra tirar o anel, só a palavra).
- 2026-07-14 (acesso direto ao painel): a fundadora pediu o painel disponível já na primeira tela. Botão "📊 Meu painel" adicionado na tela de boas-vindas (`_tela_boas_vindas`): antes era preciso mandar uma mensagem pra barra lateral (e o botão) aparecerem. Primeira versão saiu centralizada abaixo da pergunta e a fundadora rejeitou (protagonismo demais, design de botão padrão): reposicionado como pílula fantasma fixa no canto superior direito (fundo translúcido + blur, borda arredondada 999px, hover laranja), estilizada via CSS no seletor `.st-key-painel_boas_vindas` (Streamlit 1.39+ expõe a key do elemento como classe). A ação principal da tela (dizer o que quer comprar) continua sozinha no centro. Testado com a conta `demo`: clique na pílula abre o painel populado direto; "← Voltar pro chat" devolve pra tela de boas-vindas. Segunda rodada de visual a pedido dela ("mais moderno, mais chamativo", mantendo a posição secundária): borda em gradiente laranja→dourado (truque dos dois backgrounds, padding-box + border-box), halo laranja pulsando em loop suave (`@keyframes pulso-painel`), hover com brilho maior e leve subida.
- 2026-07-14 (carga de teste): criado `criar_dados_demo.py`, script reutilizável que cria/remove contas fictícias no banco com 4 perfis prontos pro painel (equilibrado, impulsivo, endividado, iniciante), com datas relativas ao dia da execução (sempre caem no mês corrente). Uso: `criar_dados_demo.py <nome> <perfil>`, `--remover`, `--listar`. Contas de demonstração ativas no banco: `demo` (equilibrado), `marina` (impulsivo), `roberto` (endividado), `patricia` (iniciante) — remover quando não precisar mais.
- 2026-07-14 (favicon): trocado o ícone da aba do navegador, que usava o emoji 🛡️ (padrão do `st.set_page_config`), pelo símbolo de verdade da marca (`assets/icone.png`, os blocos com o quadradinho laranja). Confirmado via `<link rel="icon">` e fetch direto do arquivo servido pelo Streamlit.
- 2026-07-15 (revisão de UX premium na tela inicial): a pedido dela, revisão como design sênior na tela de boas-vindas. Achados: (1) vazio gigante no celular entre a pergunta e a barra de digitar (bug real, `padding-top: 20vh` não considerava a barra fixa embaixo), (2) logo pesada demais pra tela vazia, (3) pergunta deveria ser a protagonista visual e não é, (4) pulso do botão do painel em tensão com o pedido anterior de "mais secundário". Ela pediu pra resolver o (1) primeiro.
  - **Causa raiz encontrada via inspeção do DOM** (não só olhando print): o container principal do Streamlit (`stMainBlockContainer`) é filho de um flex column, mas fica junto de um spacer interno do próprio Streamlit (uma `div` sem testid) que competia pelo mesmo espaço flexível e ficava com a maior parte dele. E mesmo depois de corrigir isso, o `justify-content:center` não tinha efeito porque o filho direto (`stVerticalBlock`, também `flex:1` por padrão do Streamlit) já preenchia 100% da altura disponível — a centralização real precisava estar no `justify-content` desse `stVerticalBlock`, não no pai.
  - Trocado o `padding-top: 20vh` chutado por CSS injetado (escopado só a esta tela, via `<style>` que só existe enquanto `_tela_boas_vindas` roda) que faz o container real crescer (`flex:1`) pra preencher exatamente o espaço entre o topo e a barra fixa, zera o spacer concorrente, e centraliza dentro do `stVerticalBlock`.
  - Testado nos três tamanhos (celular 375px, tablet 768px, desktop 1280px): conteúdo centralizado de verdade nos três, sem vazio nem scroll.
  - **Itens 2 e 3 do review** (logo enxuta + pergunta como protagonista): trocado o lockup completo (símbolo + "Guardião do Bolso" + "BY IAGILIZE" + tracinho) por só o símbolo, discreto (opacidade 0.85, 40px). A pergunta "O que você quer comprar hoje?" saiu de 19px normal pra 30px/700 (peso de manchete), virando o elemento dominante da tela. O lockup completo continua intacto em `_cabecalho_logo()` (chat, painel, formulário de perfil): a simplificação é só nesta tela de abertura, que fica vazia por definição. Testado nos mesmos três tamanhos.
  - **Item 4** (tensão "secundário" x "chamativo" no botão): resolvido a favor de secundário, por decisão da fundadora. Removida a animação de pulso contínuo (`@keyframes pulso-painel`) e reduzido o brilho parado (`box-shadow` de 18% pra 18%→10px, mais discreto); o brilho forte ficou reservado só pro hover, como resposta a interação, não como chamariz constante. Confirmado via `getComputedStyle` que `animationName` ficou `none`.
- 2026-07-14: **enviado pro GitHub** (commit `005ed1c`, branch `main`), fundação de dados (12/07) e painel financeiro (13-14/07) juntos num push só. Streamlit Community Cloud redeploy automático a partir do push. Ainda não confirmado visualmente em produção (só testado local até aqui).

## 10. Próximos passos

- 2026-07-09: enviado pro GitHub (commit `653012c`, branch `main`) tudo desta sessão: logo negativa, avatares do chat, lista de mercado e o critério de categorias. Streamlit Community Cloud redeploy automático a partir do push.
- 2026-07-10: **memória migrada do SQLite local pro Supabase** (Postgres), resolvendo a limitação de banco efêmero anotada desde o deploy (07/07). Código enviado (commit `6cccd36`), `DATABASE_URL` configurada nos Secrets do Streamlit Cloud, `ModuleNotFoundError` inicial corrigido com reboot manual, confirmado funcionando em produção. Detalhes completos na seção 9.
- 2026-07-15: enviado pro GitHub (commit `41b2cbb`, branch `main`) o redesign da tela de abertura (botão do painel, correção do vazio no mobile, logo enxuta, pergunta como protagonista, favicon) e o script `criar_dados_demo.py`. Streamlit Community Cloud redeploy automático a partir do push. Ainda não confirmado visualmente em produção nesta rodada — só testado local.
- Decidir com a fundadora se aplica o resto do Design System agora (tipografia Sora/Manrope, selo de veredito nomeado nos cartões, correção do hex do azul de informação) ou deixa pra depois.
- Adicionar login de verdade (privacidade por pessoa, hoje é só um nome na URL sem senha). Depois disso, considerar reconstruir front em Next.js se quiser rodar na Vercel.
